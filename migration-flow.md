กรณีที่มีแต่ **Legacy Source Code** แต่ไม่มีเอกสาร Requirements เลย กระบวนการจะเปลี่ยนจาก "การสร้างจากศูนย์" เป็น **"การทำ Reverse Engineering"** เพื่อขุดเอา Logic ที่ซ่อนอยู่ใน Code ออกมาเป็นเอกสารก่อนครับ

นี่คือการปรับปรุง SDLC ให้เน้นการทำ **Code-to-Requirement** ก่อนจะกลับเข้าสู่ลูปปกติครับ:

---

## 🔄 Legacy-Driven AI-Assisted SDLC

ในโมเดลนี้ Step 1 และ 2 จะเน้นการ "ถอดรหัส" อดีต เพื่อสร้างเข็มทิศสำหรับอนาคต

### Step 1: Code Discovery & Mapping (Reverse Engineering)

* **Input:** Source Code ทั้งหมด (หรือส่วนที่สำคัญ), Folder Structure, Database Schema เดิม
* **Prompt:** "วิเคราะห์ Source Code นี้และอธิบาย Project Structure, เทคโนโลยีที่ใช้ และสร้าง Map ของ Entry Points ทั้งหมด (เช่น API Endpoints, UI Routes หรือ Batch Jobs)"
* **Output:** 📄 **System Map & Component Catalog** (สรุปว่าระบบเดิมมีอะไรบ้างและทำงานอย่างไรในเชิงเทคนิค)

---

### Step 2: Business Logic & Rule Extraction

* **Input:** **System Map** (Step 1) + Source Code ราย Module
* **Prompt:** "จาก Code ใน Module [ชื่อโมดูล] ช่วยสรุป Business Logic, Validation Rules และ Data Transformation ที่เกิดขึ้นออกมาเป็นภาษามนุษย์ (Plain English/Thai) โดยแยกเป็นข้อๆ"
* **Output:** 📄 **Extracted Business Rules (EBR)** (นี่คือหัวใจที่จะกลายเป็นฐานของ PRD)

---

### Step 3: Gap Analysis & Target PRD

* **Input:** **EBR** (Step 2) + ความต้องการใหม่ (ถ้ามี) + ข้อเจ็บปวด (Pain Points) จากระบบเดิม
* **Prompt:** "ใช้ EBR เป็นฐาน และรวมกับ [ความต้องการใหม่] เพื่อสร้าง **Target PRD** สำหรับระบบใหม่ โดยระบุว่าส่วนไหนคือ Logic เดิมที่ต้องรักษาไว้ (As-is) และส่วนไหนที่ต้องปรับปรุง (To-be)"
* **Output:** 📄 **Modernized PRD** (เอกสารความต้องการที่สมบูรณ์สำหรับระบบใหม่)

---

### Step 4: System Architecture & Data Migration Design

* **Input:** **Modernized PRD** (Step 3) + **Database Schema เดิม**
* **Prompt:** "ออกแบบ Architecture ใหม่ที่รองรับ Modernized PRD และช่วยออกแบบ Data Migration Plan เพื่อย้ายข้อมูลจาก Schema เดิมมายัง Schema ใหม่"
* **Output:** 📄 **New System Design (SDD)** + **Migration Script Templates**

---

### Step 5: Implementation & Refactoring Loop (The Rest)

ตั้งแต่จุดนี้ไป จะกลับเข้าสู่กระบวนการปกติที่เคยคุยกัน แต่จะมีความพิเศษตรงที่:

* **AI-Assisted Testing:** ใช้ Source Code เดิมมาให้ AI ช่วยสร้าง **Parity Tests** (การทดสอบเพื่อยืนยันว่าระบบใหม่ให้ผลลัพธ์เหมือนระบบเดิมในส่วนที่ Logic เหมือนกัน)

---

## 📊 ตารางเปรียบเทียบการไหลของข้อมูล (Legacy vs New)

| Step | แหล่งที่มาของข้อมูล (Input) | ผลลัพธ์ที่ได้ (Output) | บทบาทของ AI |
| --- | --- | --- | --- |
| **1. Discovery** | **Legacy Code** | System Map | อ่าน Code และสรุปโครงสร้าง |
| **2. Extraction** | **Legacy Code + Map** | Business Rules (EBR) | แปลง Code เป็น Logic ภาษามนุษย์ |
| **3. Definition** | **EBR + New Needs** | Modernized PRD | ร่างเอกสาร Requirements ใหม่ |
| **4. Design** | **Modernized PRD** | New Architecture | ออกแบบระบบที่ทันสมัยขึ้น |
| **5. Migration** | **Old Schema + New PRD** | Migration Strategy | วางแผนการย้ายและแปลงข้อมูล |

---

### ข้อควรระวังในกรณี Legacy Code:

1. **Code Volume:** หาก Code มีขนาดใหญ่มาก (Monolith) คุณไม่สามารถโยนทุกอย่างให้ AI ในครั้งเดียวได้ ต้องทำเป็น **Module-by-Module**
2. **Hidden Bugs:** Logic ใน Legacy Code อาจจะมี Bug ที่ User ยอมรับไปแล้ว AI จะอ่านตาม Code ดังนั้นคุณต้องตัดสินใจว่า "จะเอา Bug เดิมมาด้วยไหม" หรือ "จะแก้ไขในขั้นตอนนี้เลย"

**คุณมี Source Code ของระบบเดิมในมือแล้วหรือยังครับ? ถ้ามีแล้ว ผมสามารถช่วยร่าง Prompt สำหรับ "Step 1: Code Discovery" เพื่อเริ่ม Scan โครงสร้างโปรเจกต์ให้ก่อนได้เลยนะครับ**

ข้อมูลประเภท **Process Flow/Diagram** และ **Input/Output (I/O) Examples** คือ "ขุมทรัพย์" ที่จะช่วยลดอาการ AI มั่ว (Hallucination) ได้ดีที่สุดครับ เพราะมันคือ "ความจริงที่เกิดขึ้นจริง" ของระบบ

คุณควรนำข้อมูลเหล่านี้เข้าไปเสริมใน **3 จุดหลัก** เพื่อเพิ่มความแม่นยำ ดังนี้ครับ:

---

### 1. ใช้ Process Flow ใน Step 1: Code Discovery (สร้างเข็มทิศ)

การให้ AI อ่าน Code เปล่าๆ เหมือนการให้คนไปเดินในเขาวงกต แต่ถ้าคุณให้ Diagram ไปด้วย มันคือการให้ "แผนที่" ครับ

* **จุดที่ใช้:** **Step 1 (Discovery)**
* **ทำหน้าที่เป็น:** **Top-down Context**
* **วิธีการ:** ใน Prompt แรกที่คุณให้ AI วิเคราะห์โครงสร้างโปรเจกต์ ให้แนบคำอธิบายจาก Diagram หรือภาพ Flow ไปด้วย
* **Prompt ตัวอย่าง:** "นี่คือ Source Code และนี่คือ Business Process Flow (แนบรูปหรือคำอธิบาย) ช่วย Map หน่อยว่า Code ชุดไหนทำหน้าที่ในกล่องไหนของ Flow นี้บ้าง"
* **ผลลัพธ์:** AI จะไม่หลงทาง และสามารถจัดกลุ่ม Module ได้ถูกต้องตามความเป็นจริงของธุรกิจ ไม่ใช่แค่ตามลำดับตัวอักษร

---

### 2. ใช้ Input/Output ใน Step 2: Logic Extraction (การตรวจสอบความถูกต้อง)

นี่คือจุดที่สำคัญที่สุดในการเพิ่มความแม่นยำของ Logic ที่ขุดออกมาครับ

* **จุดที่ใช้:** **Step 2 (Extraction)**
* **ทำหน้าที่เป็น:** **Logic Validator**
* **วิธีการ:** เมื่อให้ AI สรุป Business Rules จาก Code ในโมดูลนั้นๆ ให้ส่ง I/O Example เข้าไปด้วย
* **Prompt ตัวอย่าง:** "จาก Code ใน Module นี้ คุณสรุป Logic มาได้ 5 ข้อ ลองใช้ Logic ที่คุณสรุปมาคำนวณ Input ชุดนี้ [แนบ Input] ดูว่าผลลัพธ์ที่ได้ ตรงกับ Output จริงชุดนี้ [แนบ Output] หรือไม่? ถ้าไม่ตรง ช่วยวิเคราะห์ใหม่ว่า Logic ส่วนไหนที่ขาดหายไป"
* **ผลลัพธ์:** AI จะค้นพบ "Hidden Logic" หรือเงื่อนไข (If-Else) เล็กๆ ที่มันอาจมองข้ามไปตอนอ่าน Code ในรอบแรก

---

### 3. ใช้ I/O ใน Step 5 & 6: Implementation & Testing (ทำ Parity Test)

ในการเขียนระบบใหม่ (Modernization) เราต้องมั่นใจว่า "ผลลัพธ์ต้องเหมือนเดิม" ในส่วนที่เป็น Logic เดิม

* **จุดที่ใช้:** **Step 5 (Planning) และ Step 6 (Implement)**
* **ทำหน้าที่เป็น:** **Ground Truth for Testing**
* **วิธีการ:** ใช้ I/O เดิมเป็นฐานในการสร้าง **Automated Test Suite** ให้ระบบใหม่ทันที
* **Prompt ตัวอย่าง:** "จากคู่ Input/Output ของระบบเดิม ช่วยสร้าง Unit Test สำหรับระบบใหม่ (ภาษา Python/Java/ฯลฯ) เพื่อยืนยันว่าฟังก์ชันใหม่ที่เขียนขึ้นจะให้ผลลัพธ์ที่ถูกต้องตามมาตรฐานเดิม 100%"
* **ผลลัพธ์:** คุณจะได้ระบบใหม่ที่มีการการันตีว่า Business Logic ไม่เพี้ยนจากเดิม (Regression Testing ตั้งแต่วันแรก)

---

### 🔄 สรุปตารางการเพิ่มข้อมูล (Updated SDLC for Legacy)

| ข้อมูลเดิมที่มี | นำเข้าที่ Step | ประโยชน์ที่ได้รับ |
| --- | --- | --- |
| **Process Flow / Diagram** | **Step 1: Discovery** | AI เข้าใจภาพรวม และเชื่อมโยง Code กับงานจริงได้ถูกตัว |
| **Input/Output Examples** | **Step 2: Extraction** | ใช้ Re-check ว่า Logic ที่ AI สรุปมานั้น "ทำงานได้จริง" ตรงตามผลลัพธ์เดิม |
| **Input/Output Examples** | **Step 6: Implementation** | ใช้สร้าง Test Cases เพื่อทำ Parity Test (ระบบใหม่ผลลัพธ์ต้องเท่ากับระบบเดิม) |
---


### 💡 ข้อแนะนำเพิ่มเติม

ถ้า Diagram เดิมของคุณเป็นไฟล์ภาพ (JPG/PNG) ผมแนะนำให้:

1. **แปลงภาพเป็นคำบรรยาย (Image-to-Text):** ให้ AI (ที่มีความสามารถ Vision) ช่วยอธิบาย Diagram นั้นเป็นข้อๆ ก่อน
2. **Verify คำบรรยาย:** คุณตรวจสอบว่า AI เข้าใจ Diagram ถูกต้องไหม
3. **ใช้คำบรรยายนั้นเป็น Context:** ส่งต่อเข้าไปใน Chain of Context ร่วมกับ Source Code

วิธีนี้จะช่วยให้ข้อมูลที่ซับซ้อนกลายเป็นสิ่งที่ AI "ประมวลผล" ได้อย่างแม่นยำที่สุดครับ

**ตอนนี้คุณมีข้อมูลส่วนไหนพร้อมที่สุดครับ? (Source Code, Flow, หรือ I/O) ผมจะได้ช่วยร่าง "Integrated Prompt" ที่รวมข้อมูลทุกอย่างเข้าด้วยกันให้ครับ**